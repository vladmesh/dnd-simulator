# E2E Report: sprint020-phase4 (frontend decomposition)

**Date:** 2026-07-04
**Flags:** --no-llm
**Sections tested:** targeted (frontend-touching surfaces of Phase 4)
**Stack:** LOG_LEVEL=DEBUG, LOG_DIR=/tmp/dnd-e2e-logs, backend :8001 + vite :5173 from the phase-4 worktree

## Summary

- Scenarios: 12 tested, 12 passed, 0 failed
- Quick fixes: 0
- Blockers: 0

Phase 4 is a behavior-preserving frontend decomposition (TargetDropdown, SchemaForm, EventLog,
WorldOverview, shared types, store/transport dedup). E2E targeted every UI surface the refactor
touched. All behaved identically to the pre-refactor baseline.

## Results

### Session setup + store (tasks 5, 6)

| # | Scenario | Status | Notes |
|---|----------|--------|-------|
| 1.1 | Landing page split | pass | Play / Dungeon Master cards |
| 1.2 | Create Paladin, enter game | pass | connect() reset + onTurn: game time `Y1490 M6 D1 10:00` shown (extractGameTime), unified PlayerStatus renders (STR 15, AC 18, Paladin L1) |
| 1.5 | Class-specific creation UI | pass | Paladin L1 correctly shows NO Fighting Style selector (L2 feature, chosen at level-up) |

### Combat + TargetDropdown/SmiteChoice (tasks 1, 3, 5, 6)

| # | Scenario | Status | Notes |
|---|----------|--------|-------|
| 3.1/3.2 | Attack xp_dummy → combat | pass | Attack breakdown RU `[d20(19)+4=23 vs КЗ 9], 3 урона (1d8 рубящий + +2 str)`; reputation_changed + xp_gained events |
| 3.5 | Level-up L1→L2 | pass | Auto-opened LevelUpModal (Max HP +6, "Gains 2 Level 1 spell slots", Fighting Style Defense/Dueling/GWF, OK disabled until pick); after Confirm: L2, HP 10→16, Spell Slots Lv1 panel appears |
| 14.3 | Divine Smite via TargetDropdown | pass | ActionBar Attack → shared `<SmiteChoice>` menu ("Attack …", "Attack … + Smite (slot 1) (2/2)", "Cancel"); smite fired `9 урона (1d8 рубящий + 2d8 divine_smite + +2 str + +2 дуэлянт)`; slot consumed, combat_ended → sidebar back to peaceful |
| 3.4 | Combat ends | pass | entity_died, "Бой окончен.", Loot panel with corpses |

### Event log (task 3)

| # | Scenario | Status | Notes |
|---|----------|--------|-------|
| 10.x | Log rendering (compact + overlay) | pass | Round headers ("Round 1/2"), turn headers (per actor), aggregated move ("Practice Thug moved (10 ft)"), clickable attack rows, crit shown; useStickyScroll/useLogInteraction intact |

### Master / WorldOverview (tasks 4, 5)

| # | Scenario | Status | Notes |
|---|----------|--------|-------|
| 6.1 | Worlds tab | pass | editable (Fork+Delete) vs library (Fork) |
| 6.x | World Overview — Regions/Nations/Settlements | pass | Regions read-only (no Actions column), weather `clear, 21°` (WeatherSummary render); Settlement Region column resolves region_id → region name; typed Region/Nation/Settlement rows |
| 6.x | Inline nation edit | pass | Edit → 3 spinbuttons + Save/✕; changed Wealth 65→80 → patchNation saved, row back to read mode (EditableStatsTable optimistic-save) |

### SchemaForm (task 2)

| # | Scenario | Status | Notes |
|---|----------|--------|-------|
| 6.4 | World editor stepper + entity form | pass | "Edit arena" region form: FieldShell labels with required `*`, enum→select (TerrainType), number fields, array-of-strings (Connections), nested-object fieldset (BattleMapContent). Identical rendering to baseline |

## Findings

### Blockers
- None.

### Minor
- **Post-level-up one-frame smite race (pre-existing, NOT introduced by this refactor).** Immediately after confirming L2 in the same turn, the first ActionBar Attack click sent a plain attack (no smite menu) — the combat awareness had not yet carried the new `spell_slot_1` at that exact frame. On the next settled turn the SmiteChoice menu appeared and smite fired correctly. The smite-detection logic (`getSpellSlots`/`hasSmiteOption`) is byte-identical to the pre-refactor code, so this is not a regression. Not worth a fix (transient, self-corrects next turn).

## Log Analysis

- Backend log: 0 errors / exceptions / tracebacks.
- Browser console: 0 errors; 1 benign warning (`WebSocket closed before connection established` during the initial connect handshake — pre-existing dev behavior, wsClient.ts:30).
