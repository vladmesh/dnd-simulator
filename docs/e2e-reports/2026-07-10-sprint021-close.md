# E2E Report: sprint021-close

**Date:** 2026-07-10
**Flags:** --no-llm
**Scope:** post-audit regression smoke
**Stack:** uvicorn :8001, vite :5173, `LOG_LEVEL=DEBUG`, `LOG_DIR=/tmp/dnd-e2e-logs`

## Summary

- Scenarios: 4 requested smoke areas, 2 passed, 1 partial, 1 blocked by target/session instability.
- Quick fixes: 0, code was not changed.
- Blockers: 1 E2E blocker in the combined player flow.

## Results

| # | Scenario | Status | Notes |
|---|---|---|---|
| 1 | Create character -> movement -> combat kill -> XP/loot | partial | Character creation and movement to Silverport Market worked. Combat/loot was unstable because the UI exposes several identical `Attack` buttons; automation hit Gretta in one run instead of the spawned test creature. XP was verified separately on `level_up_test`: killing `xp_dummy` awarded 500 XP and opened level-up. |
| 2 | Quick save -> load sanity | pass | Save `sprint021_smoke_1783701831851` was listed, file had `schema_version=1`, and load restored time from `Y1490 M6 D1 13:02` to `Y1490 M6 D1 10:02`. |
| 3 | Trading | pass earlier, unstable after contamination | Clean run passed: buy/sell with Gretta changed gold `1000 -> 950 -> 1000`. After a diagnostic run accidentally killed Gretta, a later fresh player session in the same backend process reached Silverport Market with no nearby entities and stayed `Waiting for turn...`; backend restart was needed. |
| 4 | Master panel time/spawn creature | pass | `/master/:sessionId` Creatures tab spawned `Smoke Spawn`; Time tab advanced from `Y1490 M6 D1 10:00` to `Y1490 M6 D2 10:00`. |

## Findings

### Blockers

- Combined player smoke is not reliable through the current UI because repeated `Attack` buttons have the same accessible name. Repro: create a Sword Vale fighter, move to Silverport Market, add a test creature near Gretta, then target by role/name. Playwright sees Gretta, the test creature, and the action-bar attack as indistinguishable `Attack` controls; one run attacked and killed Gretta, which then polluted later in-process sessions.

### Minor

- Mixed locale text appears in EN UI during combat/logs: e.g. Russian combat lines and `человек` labels on an English screen.
- `SAVES_DIR=/tmp/dnd-e2e-saves` was ignored by the API app; saves were written under repo `saves/`.
- Live-spawned generic monsters do not accept `xp_value` through the master spawn API, so XP smoke used the existing `level_up_test` fixture.

## Log Analysis

- No backend `error`, `exception`, or `traceback` lines were found in `/tmp/dnd-e2e-backend.log`.
- Expected `llm_not_configured_fallback` warnings appeared for LLM NPCs; LLM scenarios were out of scope.
- Session logs confirm XP fixture behavior: `XP Smoke` killed `XP Dummy`, received 500 XP, and `level_up_available=true`.

## Artifacts

- Playwright screenshots and JSON: `/tmp/dnd-e2e-sprint021-close/`
- Backend log: `/tmp/dnd-e2e-backend.log`
- Frontend log: `/tmp/dnd-e2e-frontend.log`
