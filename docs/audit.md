# Code Audit

> **Date**: 2026-06-29
> **Scope**: full (post Sprint 020 — control-interfaces, all 4 phases closed)

> _Transient snapshot — regenerated by `/audit` each run. Canonical tracking lives in [BACKLOG.md](BACKLOG.md); `/audit-triage` reconciles findings into it._

## Summary
- Dead code: 0 issues
- Code smells: 2 issues (1 new-this-sprint low, 1 pre-existing/backlogged)
- Security: 2 issues (1 new by-design low, 1 pre-existing/backlogged)
- Architecture violations: 0 issues
- Convention violations: 2 issues (both backlogged `Any`-sweep)
- Layer contract: 0 issues
- Test gaps: 0 new (all map to existing `test-gap-*` backlog entries)
- Vision drift: 0 issues

**Total: 6 issues, 1 genuinely new (WS rate-limit copy-paste, low).** Sprint 020 met its goal cleanly: identity/role keystone (`service/identity.py`, pure + frozen), three-lens projection (projection-only, no enforcement), spectator-listener + disconnect grace-period (`threading.Timer`, properly locked), and the save/i18n cluster (`player-xp-not-persisted`, `combat-log-i18n-gaps`) all closed with new tests. No architecture, layer-contract, or dependency-flow drift; `LlmClient` still constructed only at the `app.py` injection point; `rules/` still pure (no I/O, no network, no `self.`-state). Backend lint (`ruff F401`) clean, no `TODO/FIXME`, no lines over 120, no new `Any`-without-`object` outside the two already-backlogged raw-YAML sites.

## Dead Code
| File | Issue | Action |
|------|-------|--------|
| — | `ruff F401` clean; no zero-caller functions; no orphan modules; no `TODO/FIXME/HACK/XXX`. | none |

## Code Smells
| File | Issue | Suggestion |
|------|-------|------------|
| `adapters/api/routes_ws.py:101-117, 198-215` | **New this sprint.** Rate-limit token-bucket constants (`20.0 / 20.0 / 5.0`) + the budget/refill loop are copy-pasted between `_run_spectator` and `websocket_game` — the spectator path duplicated the player path. | Extract a tiny `TokenBucket` helper (or a shared receive-loop). Low priority; both paths are correct. |
| `service/session.py:610`, `layers/entities/layer.py:631`, `round.py:619`, `activation_manager.py:614`, `perception.py:572` | Files over 400 lines. `session.py` grew with the spectator + grace-period code (≈536 → 610). | Pre-existing stateful orchestrators; backlogged (`round-growing`, `activation-manager-growing`, `god-class-combat-manager`, `test-gap-session`). `session.py` is the one to watch — if it grows again, peel listener/spectator lifecycle into a mixin (mirrors the GameService peel). Not urgent. |

## Security
| File:Line | Issue | Severity |
|-----------|-------|----------|
| `service/identity.py` + `adapters/api` `get_identity` seam | **New this sprint, by design.** `X-User-Id` / `X-Role` headers are trusted with no authentication; blank role defaults to `ADMIN` (god-mode). Any client can claim any identity/role. | low — documented in the module docstring ("No passwords, no login, no DB — minimal weight for Sprint 020"). Must be hardened (real auth) before any non-local deployment. Consistent with the `creator`-is-attribution-not-access model. |
| `adapters/api/app.py:91-92`, `routes_ws.py:142` | `allow_methods=["*"]` / `allow_headers=["*"]` hardcoded (origins configurable via `CORS_ALLOWED_ORIGINS`); WS session id in URL with no auth. | low — pre-existing, backlogged `cors-wildcard`. The new `?spectate=true` path is read-only (rejects `action`/`reaction`, never evicts), so it does not widen the write surface. |

Verified clean this pass: no hardcoded secrets, `.env` gitignored, no `subprocess`, no `dangerouslySetInnerHTML`. WS keeps origin allow-list + token-bucket rate limit on both player and spectator paths, and validates JSON before dispatch (`Invalid JSON` reject). `LlmClient(` constructed only at `app.py:71`.

## Architecture Violations
| File:Line | Violation | Should Be | Severity |
|-----------|-----------|-----------|----------|
| — | none | — | — |

Cross-layer imports: only own-package (`politics→politics.models`, etc.). `core/` and `rules/` import nothing upward. `adapters/` import only enums/schemas/constants (`BrainType`, `FightingStyle`, `POINT_BUY_BUDGET`/`STARTING_GOLD`, `dice.set_global_seed`) — enum/const-at-boundary, not business logic. `rules/` has zero `os/sys/json/open/print`, zero network, zero `self.`-state. `session.py` background-thread state is guarded by `_lock`; `_fire` snapshots listeners under the lock then dispatches outside it; the evict `threading.Timer` is armed/cancelled/re-verified under the lock with stop+`_on_empty` fired outside it to avoid deadlock. The lazy `core/ → rules/` helper imports (`class_features`, `combat`, `monster`) remain by-design composition (frozen core delegates math to pure rules), not a runtime cycle.

## Convention Violations
| File:Line | Violation | Rule |
|-----------|-----------|------|
| `content_loader/monsters.py:129` (`_parse_encounter_entries`) | `: Any` param at raw-YAML boundary | Backlogged `any-encounter-entries`; `object`/`list[object]` is stricter. |
| `content_loader/refs.py:62` (`_resolve_name`) | `: Any` param at dynamic content-model boundary | Part of backlogged `any-to-object-sweep`. |

Line length clean (0 lines >120, ruff-enforced). Bare `@dataclass` (non-frozen) instances are all intentionally stateful runtime objects (`GameSession`, `Round`, entities/HP, `CombatState`, `TurnBudget`, `ResourcePool`, `Lair`, `Container`); no new mutable model dataclasses introduced this sprint.

## Layer Contract
| Layer | Issue |
|-------|-------|
| — | All 5 layers implement the full ABC; mypy strict passes. No incomplete `handle_event`/`query` stubs. |

## Test Gaps
| Source File | Expected Test | Status |
|-------------|---------------|--------|
| `rules/fighting_style.py` | dedicated unit test | backlogged `test-gap-fighting-style` (indirect via `test_second_wind`/`test_create_player`); only zero-reference rules module |
| `routes_ws.py` | malformed-JSON / disconnect-during-loop | backlogged `test-gap-ws-disconnect` etc. (player path; spectator JSON-reject + action-reject are covered) |
| Sprint 020 new code | — | **covered**: `test_identity.py`, `test_session_lifecycle.py` (spectator/evict/grace), integration `test_websocket.py` (spectator endpoint), `test_commands_save.py` (XP persist), `test_reputation_event_faction_name.py`, `test_handler_error_i18n.py` |

No skipped/xfail tests. The bulk "missing `test_rules_*`" hits are the skill's naming-convention false positive (repo names tests `test_combat.py`, not `test_rules_combat.py`); genuine gaps are already tracked as `test-gap-*` in BACKLOG.

## Vision Drift
| Change | Invariant Violated | Impact |
|--------|-------------------|--------|
| — | None. Identity/roles are projection-only (no enforcement, no 403s) and route master influence through the service layer — consistent with "Master controls through endpoints only". Spectator-listener is read-only and never drives the round, so single-global-round and brain-swappability hold. Classic (RuleBrain) mode untouched; content stays in YAML. | — |

## Backlog hygiene (for `/audit-triage`)
- `session-disconnect-debounce` (BACKLOG.md:68) is still `[ ] **should**` but was **FIXED** in Sprint 020 Phase 3 task 2 (deferred `threading.Timer` evict; reconnect/stop cancels; `_run_evict_check` re-verifies player-empty). Mark it `[x]` with the sprint/task reference, mirroring the closed `player-xp-not-persisted` and `combat-log-i18n-gaps` entries.
