# Code Audit

> **Date**: 2026-06-28
> **Scope**: full (post Sprint 018 — lairs, encounters, loot, time-of-day)

> _Transient snapshot — regenerated by `/audit` each run. Canonical tracking lives in [BACKLOG.md](BACKLOG.md); `/audit-triage` reconciles findings into it._

## Summary
- Dead code: 0 issues
- Code smells: 4 issues
- Security: 1 issue
- Architecture violations: 0 issues
- Convention violations: 3 issues
- Layer contract: 0 issues
- Test gaps: 4 issues (1 new, rest pre-existing/backlogged)
- Vision drift: 0 issues

**Total: 12 issues** (mostly low/could). Healthy post-sprint state: no architecture or layer-contract drift, security hardened since the 2026-04-13 audit (CORS now configurable, WS gained origin-check + token-bucket rate limiting), Sprint 018 code on-vision and well-tested.

## Dead Code
| File | Issue | Action |
|------|-------|--------|
| — | `ruff F401` clean, no `TODO/FIXME/HACK`, no orphan modules found | none |

## Code Smells
| File | Issue | Suggestion |
|------|-------|------------|
| `service/game_service.py` (1044) | God-class facade — largest file, still growing | Already split into `commands_*` modules; keep peeling query/command groups off the facade. Pre-existing, ongoing. |
| `layers/entities/activation_manager.py` (614) | Grew with encounter rolling + time-of-day filter (`_roll_encounters`, `_is_daylight_at`) | Candidate to extract an `EncounterRoller` helper, mirroring how `combat_manager`/`awareness_builder` were peeled off the layer. |
| `layers/entities/layer.py` (629) | Entities layer is the largest layer; orchestrates activation, awareness, combat, materialization | Mostly delegates already; watch for further growth as ecology features land. |
| `frontend EventLog.tsx:335`, `master/SchemaForm.tsx:137` | `// eslint-disable-next-line react-hooks/exhaustive-deps` ×2 | Acceptable (intentional effect deps); revisit if either effect is touched. |

## Security
| File:Line | Issue | Severity |
|-----------|-------|----------|
| `adapters/api/schemas.py:87` | Item-creation/give schema fields `base_ac`, `max_dex_bonus`, `strength_req`, `ac_bonus`, `reach` accept arbitrary `int` (no `Field(ge=, le=)` bounds) unlike the player HP/AC fields. Master-only endpoint, game-data only. | low |

Hardened and verified clean this pass:
- CORS: `allow_origins=_cors_origins` (configurable via `CORS_ALLOWED_ORIGINS`), `allow_credentials` disabled when wildcard present. No bare `["*"]`.
- WebSocket: origin allow-list check (`routes_ws.py:90`) + token-bucket rate limiting (`:134`) with i18n "Rate limited" reply.
- No hardcoded secrets, `.env` gitignored, no `subprocess`, no `dangerouslySetInnerHTML`, no `Any`-typed LLM prompt injection of user text.

## Architecture Violations
| File:Line | Violation | Should Be | Severity |
|-----------|-----------|-----------|----------|
| — | None. No cross-layer imports (all intra-package), `core/` & `rules/` import nothing upward, `LlmClient(` constructed only at `app.py:71`, adapters import only enums/constants/schemas. | — | — |

## Convention Violations
| File:Line | Violation | Rule |
|-----------|-----------|------|
| `content_loader/monsters.py:207` | `treasure_items: list[Any]` — `parse_items()` returns `list[Item]`; annotation is looser than the value | Use `list[Item]` (or `object` per state-dict rule). New in Sprint 018. |
| `core/turn_budget.py:18`, `core/resource.py:16` | `@dataclass` without `frozen=True` on per-turn / resource value objects | Backlogged (`mutable-turn-budget`, `mutable-resource-pool`); both are intentionally mutated. Document as stateful or move to `replace()`. |
| `content_loader/monsters.py:128` | `_parse_encounter_entries(entries: Any, ...)` | Raw-YAML boundary; `object`/`list[object]` would be stricter. Low — content_loader routinely handles dynamic input. |

Note: `Lair` and `Container` (Sprint 018) are bare `@dataclass` by design — both are runtime state machines / lootable entities that mutate (`state`, `core_alive`, `inventory`). Correct, not flagged.

## Layer Contract
| Layer | Issue |
|-------|-------|
| — | All layers implement the full ABC; mypy strict passes. No incomplete `handle_event`/`query` stubs found. |

## Test Gaps
| Source File | Expected Test | Status |
|-------------|---------------|--------|
| `rules/encounters.py:8` `is_active_at_time` | direct unit test | **new** — pure rule covered only indirectly via integration `test_time_of_day_encounters.py` (through GameService/ActivationManager). Trivial one-liner, but a pure rule with no direct unit test. `could`. |
| `rules/action_provider.py` | `tests/unit/test_action_provider.py` | pre-existing, backlogged (`test-gap-action-provider`) |
| `rules/politics.py`, `rules/settlements.py` | dedicated unit tests | pre-existing, backlogged (`test-gap-politics-rules`, `test-gap-settlements-rules`) |
| `rules/weapons.py`, `rules/leveling.py`, `rules/fighting_style.py` | dedicated unit tests | indirect coverage via combat/level tests; no named file. `fighting_style` already backlogged. |

Sprint 018 modules are well covered: `test_lair_materialization.py`, `test_lair_treasury.py`, `test_container.py`, `test_loot.py`/`test_take_loot.py`/`test_loot_awareness.py`, `test_inventory.py`/`test_inventory_awareness.py`, `test_region_encounters.py`, `test_time_of_day_encounters.py`. `rules/geography.is_daylight` covered by `test_geography_formulas.py`. No skipped/xfail tests.

## Vision Drift
| Change | Invariant Violated | Impact |
|--------|-------------------|--------|
| — | None. Sprint 018 (lairs, region/time-of-day encounters, loot) is rule-based (no LLM dependency), uses the single global round, keeps content in YAML, and implements the Kenshi-style "danger fixed by place/time, never scaled to party" invariant directly. | — |
